---
cog_bundle_version: 4
name: plugin-testing
description: a fake bundle for testing the cog-bundle-buildkite-pipeline plugin
version: 1.0.0
docker:
  image: cogcmd/plugin-testing
  tag: 1.0.0
commands:
  echo:
    executable: "/bundle/echo"
    description: Echoes what it's passed
    rules:
    - allow
